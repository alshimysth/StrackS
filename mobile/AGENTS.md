# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Note (2026-07-17): downgraded from SDK 57 to SDK 54 — the App Store build of Expo Go had not
yet been approved for SDK 57 (Apple review pending), so SDK 54 is the newest version testable
via Expo Go on a physical device without an EAS development build. Do not bump back to 57
without checking whether Expo Go has caught up.
